@model DTOs.EventoAtletaDTOs.EventoAtletaDto

@{
    ViewData["Title"] = "ActualizarPuntuacion";
}

<h1>ActualizarPuntuacion</h1>

<h4>EventoAtletaDto</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="ActualizarPuntuacion">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
               
                <input type="hidden" asp-for="Atleta.Id" class="form-control" name="idAtleta" />
                <span asp-validation-for="Atleta.Id" class="text-danger"></span>
            </div>

            <div class="form-group">
               
                <input type="hidden" asp-for="Evento.Id" class="form-control" name="idEvento" />
                <span asp-validation-for="Evento.Id" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Puntaje" class="control-label"></label>
                <input asp-for="Puntaje" class="form-control" />
                <span asp-validation-for="Puntaje" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
